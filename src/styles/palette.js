import { createTheme } from '@mui/material/styles'

export const theme = createTheme({
  palette: {
    neutral: {
      main: '#8560A9',
      contrastText: '#fff'
    },
    bright: {
      main: '#E5F7A9',
      contrastText: '#67616D'
    }
  }
})
